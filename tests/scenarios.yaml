# Generated by sgnl-test-init — fill in the TODOs below
#
# Scenarios with "record: true" will be recorded by: npx sgnl-test-record
# Fill in real secrets in action.context.secrets, then run the recorder.
# After recording, redact secrets before committing.
action:
  params:
    roleName: test-role-1
    region: us-east-1
  context:
    secrets:
      BASIC_USERNAME: username
      BASIC_PASSWORD: password
    environment: {}
scenarios:
  - name: Base Success Case
    request:
      method: POST
      url: https://iam.amazonaws.com/
    fixture: fixtures/base-success-case.http
    invoke:
      returns:
        roleName: test-role-1
        policyName: AWSRevokeOlderSessions
        applied: true
  - name: Session Already Revoked
    request:
      method: POST
      url: https://iam.amazonaws.com/
    fixture: fixtures/session-already-revoked.http
    invoke:
      returns:
        roleName: test-role-1
        policyName: AWSRevokeOlderSessions
        applied: true
  - name: Role Does Not Exist
    params:
      roleName: test-role-2
    request:
      method: POST
      url: https://iam.amazonaws.com/
    fixture: fixtures/role-does-not-exist.http
    invoke:
      throws: "Role not found: test-role-2"
  - name: AWS Credentials Invalid
    context:
      secrets:
        BASIC_USERNAME: invalid-username
        BASIC_PASSWORD: invalid-password
    request:
      method: POST
      url: https://iam.amazonaws.com/
    fixture: fixtures/aws-credentials-invalid.http
    invoke:
      throws: "Failed to apply policy: The security token included in the request is invalid."
